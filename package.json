{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "real-dev": "HOST=0.0.0.0 nuxt dev",
    "real-prod": "npm run build && HOST=0.0.0.0 npm run preview",
    "dev": "npm run real-prod",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "unplugin-auto-import": "^0.18.2",
    "nuxt": "^3.12.4"
  }
}
